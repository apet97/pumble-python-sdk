"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from jsonpath import JSONPath
from pumble_keys import models, utils
from pumble_keys._hooks import HookContext
from pumble_keys.types import BaseModel, OptionalNullable, UNSET
from pumble_keys.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Awaitable, Dict, Iterable, List, Mapping, Optional, Union, cast


class Messages(BaseSDK):
    r"""Send, edit, delete, react to, search, and list messages."""

    def send_message(
        self,
        *,
        request: Union[
            models.operations.SendMessageRequest,
            models.operations.SendMessageRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Send a message to a channel

        Sends a text (or rich-text) message. Provide **either** `channelId`
        (preferred) **or** `channel` (by name). If `threadRootId` is set, the
        message is posted as a reply in that thread (equivalent to
        `sendReply`).


        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, models.operations.SendMessageRequest)
        request = cast(models.operations.SendMessageRequest, request)

        req = self._build_request(
            method="POST",
            path="/sendMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.SendMessageRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="sendMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def send_message_async(
        self,
        *,
        request: Union[
            models.operations.SendMessageRequest,
            models.operations.SendMessageRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Send a message to a channel

        Sends a text (or rich-text) message. Provide **either** `channelId`
        (preferred) **or** `channel` (by name). If `threadRootId` is set, the
        message is posted as a reply in that thread (equivalent to
        `sendReply`).


        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, models.operations.SendMessageRequest)
        request = cast(models.operations.SendMessageRequest, request)

        req = self._build_request_async(
            method="POST",
            path="/sendMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.SendMessageRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="sendMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def send_reply(
        self,
        *,
        request: Union[
            models.operations.SendReplyRequest,
            models.operations.SendReplyRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Reply to a message in a channel (thread)

        Posts `text` as a reply in the thread rooted at `messageId`. Provide
        **either** `channelId` (preferred) **or** `channel` (by name).


        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, models.operations.SendReplyRequest)
        request = cast(models.operations.SendReplyRequest, request)

        req = self._build_request(
            method="POST",
            path="/sendReply",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.SendReplyRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="sendReply",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def send_reply_async(
        self,
        *,
        request: Union[
            models.operations.SendReplyRequest,
            models.operations.SendReplyRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Reply to a message in a channel (thread)

        Posts `text` as a reply in the thread rooted at `messageId`. Provide
        **either** `channelId` (preferred) **or** `channel` (by name).


        :param request: The request object to send.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        if not isinstance(request, BaseModel):
            request = utils.unmarshal(request, models.operations.SendReplyRequest)
        request = cast(models.operations.SendReplyRequest, request)

        req = self._build_request_async(
            method="POST",
            path="/sendReply",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.SendReplyRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="sendReply",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def dm_user(
        self,
        *,
        user_id: str,
        text: str,
        as_bot: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Send a direct message to a user

        Opens (or reuses) a 1-to-1 DM channel and posts a message. The
        response's `channelId` is the DM channel — useful for follow-up
        operations.


        :param user_id:
        :param text:
        :param as_bot:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.DmUserRequest(
            user_id=user_id,
            text=text,
            as_bot=as_bot,
        )

        req = self._build_request(
            method="POST",
            path="/dmUser",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.DmUserRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="dmUser",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def dm_user_async(
        self,
        *,
        user_id: str,
        text: str,
        as_bot: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Send a direct message to a user

        Opens (or reuses) a 1-to-1 DM channel and posts a message. The
        response's `channelId` is the DM channel — useful for follow-up
        operations.


        :param user_id:
        :param text:
        :param as_bot:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.DmUserRequest(
            user_id=user_id,
            text=text,
            as_bot=as_bot,
        )

        req = self._build_request_async(
            method="POST",
            path="/dmUser",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.DmUserRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="dmUser",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def dm_group(
        self,
        *,
        user_ids: Iterable[str],
        text: str,
        as_bot: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Send a direct message to a group of users

        Opens (or reuses) a multi-party DM channel and posts a message.

        :param user_ids:
        :param text:
        :param as_bot:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.DmGroupRequest(
            user_ids=utils.unmarshal(user_ids, List[str]),
            text=text,
            as_bot=as_bot,
        )

        req = self._build_request(
            method="POST",
            path="/dmGroup",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.DmGroupRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="dmGroup",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def dm_group_async(
        self,
        *,
        user_ids: Iterable[str],
        text: str,
        as_bot: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.MessageRef:
        r"""Send a direct message to a group of users

        Opens (or reuses) a multi-party DM channel and posts a message.

        :param user_ids:
        :param text:
        :param as_bot:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.DmGroupRequest(
            user_ids=utils.unmarshal(user_ids, List[str]),
            text=text,
            as_bot=as_bot,
        )

        req = self._build_request_async(
            method="POST",
            path="/dmGroup",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.DmGroupRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="dmGroup",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.MessageRef, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def fetch_message(
        self,
        *,
        message_id: str,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.Message:
        r"""Fetch a single message by ID

        :param message_id:
        :param channel_id:
        :param channel:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.FetchMessageRequest(
            message_id=message_id,
            channel_id=channel_id,
            channel=channel,
        )

        req = self._build_request(
            method="GET",
            path="/fetchMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="fetchMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.Message, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def fetch_message_async(
        self,
        *,
        message_id: str,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.Message:
        r"""Fetch a single message by ID

        :param message_id:
        :param channel_id:
        :param channel:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.FetchMessageRequest(
            message_id=message_id,
            channel_id=channel_id,
            channel=channel,
        )

        req = self._build_request_async(
            method="GET",
            path="/fetchMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="fetchMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.Message, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def fetch_thread_replies(
        self,
        *,
        root_message_id: str,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = 100,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.operations.FetchThreadRepliesResponse]:
        r"""Fetch the replies of a thread

        Returns the replies posted in the thread rooted at `rootMessageId`,
        as a flat array. Supports cursor pagination via the last reply's `id`.


        :param root_message_id:
        :param channel_id:
        :param channel:
        :param cursor: ID of the last reply on the previous page.
        :param limit:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.FetchThreadRepliesRequest(
            root_message_id=root_message_id,
            channel_id=channel_id,
            channel=channel,
            cursor=cursor,
            limit=limit,
        )

        req = self._build_request(
            method="GET",
            path="/fetchThreadReplies",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="fetchThreadReplies",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> Optional[models.operations.FetchThreadRepliesResponse]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            next_cursor = JSONPath("$[-1].id").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None

            return self.fetch_thread_replies(
                root_message_id=root_message_id,
                channel_id=channel_id,
                channel=channel,
                cursor=next_cursor,
                limit=limit,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.operations.FetchThreadRepliesResponse(
                result=unmarshal_json_response(List[models.Message], http_res),
                next=next_func,
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def fetch_thread_replies_async(
        self,
        *,
        root_message_id: str,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = 100,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.operations.FetchThreadRepliesResponse]:
        r"""Fetch the replies of a thread

        Returns the replies posted in the thread rooted at `rootMessageId`,
        as a flat array. Supports cursor pagination via the last reply's `id`.


        :param root_message_id:
        :param channel_id:
        :param channel:
        :param cursor: ID of the last reply on the previous page.
        :param limit:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.FetchThreadRepliesRequest(
            root_message_id=root_message_id,
            channel_id=channel_id,
            channel=channel,
            cursor=cursor,
            limit=limit,
        )

        req = self._build_request_async(
            method="GET",
            path="/fetchThreadReplies",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="fetchThreadReplies",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> (
            Awaitable[Optional[models.operations.FetchThreadRepliesResponse]]
        ):
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            async def empty_result():
                return None

            next_cursor = JSONPath("$[-1].id").parse(body)

            if len(next_cursor) == 0:
                return empty_result()

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return empty_result()

            return self.fetch_thread_replies_async(
                root_message_id=root_message_id,
                channel_id=channel_id,
                channel=channel,
                cursor=next_cursor,
                limit=limit,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.operations.FetchThreadRepliesResponse(
                result=unmarshal_json_response(List[models.Message], http_res),
                next=next_func,
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def search_messages(
        self,
        *,
        text: Optional[str] = None,
        from_: Optional[Iterable[str]] = None,
        in_: Optional[Iterable[str]] = None,
        limit: Optional[int] = 10,
        strategy: Optional[models.SearchStrategy] = None,
        before_ts: Optional[int] = None,
        after_ts: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.operations.SearchMessagesResponse]:
        r"""Search messages

        Searches messages by free text, sender, channel, and/or time window.
        At least one of `text`, `from`, or `in` is required.

        ## Pagination
        Cursor input is `beforeTs` (epoch ms); the next cursor is the
        `timestampMilli` of the oldest hit in the current page. **Edge case**:
        Pumble returns timestamps truncated to seconds, so messages sharing
        the same `timestampMilli` that straddle a page boundary can be
        skipped. For low-volume channels or `limit >= 10`, this is rarely
        observed; for high-volume bursts use a smaller search window via
        `afterTs` to bound the result set.


        :param text:
        :param from_: Filter by sender user IDs.
        :param in_: Filter by channel IDs or names.
        :param limit:
        :param strategy:
        :param before_ts: Restrict to messages with `timestampMilli` strictly less than this value (also the pagination cursor).
        :param after_ts: Restrict to messages with `timestampMilli` strictly greater than this value.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.SearchMessagesRequest(
            text=text,
            from_=utils.unmarshal(from_, Optional[List[str]]),
            in_=utils.unmarshal(in_, Optional[List[str]]),
            limit=limit,
            strategy=strategy,
            before_ts=before_ts,
            after_ts=after_ts,
        )

        req = self._build_request(
            method="POST",
            path="/searchMessages",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.SearchMessagesRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="searchMessages",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> Optional[models.operations.SearchMessagesResponse]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            next_cursor = JSONPath("$.content[-1].timestampMilli").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None
            results = JSONPath("$.content").parse(body)
            if len(results) == 0 or len(results[0]) == 0:
                return None

            return self.search_messages(
                text=text,
                from_=from_,
                in_=in_,
                limit=limit,
                strategy=strategy,
                before_ts=next_cursor,
                after_ts=after_ts,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.operations.SearchMessagesResponse(
                result=unmarshal_json_response(models.SearchMessagesResult, http_res),
                next=next_func,
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def search_messages_async(
        self,
        *,
        text: Optional[str] = None,
        from_: Optional[Iterable[str]] = None,
        in_: Optional[Iterable[str]] = None,
        limit: Optional[int] = 10,
        strategy: Optional[models.SearchStrategy] = None,
        before_ts: Optional[int] = None,
        after_ts: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.operations.SearchMessagesResponse]:
        r"""Search messages

        Searches messages by free text, sender, channel, and/or time window.
        At least one of `text`, `from`, or `in` is required.

        ## Pagination
        Cursor input is `beforeTs` (epoch ms); the next cursor is the
        `timestampMilli` of the oldest hit in the current page. **Edge case**:
        Pumble returns timestamps truncated to seconds, so messages sharing
        the same `timestampMilli` that straddle a page boundary can be
        skipped. For low-volume channels or `limit >= 10`, this is rarely
        observed; for high-volume bursts use a smaller search window via
        `afterTs` to bound the result set.


        :param text:
        :param from_: Filter by sender user IDs.
        :param in_: Filter by channel IDs or names.
        :param limit:
        :param strategy:
        :param before_ts: Restrict to messages with `timestampMilli` strictly less than this value (also the pagination cursor).
        :param after_ts: Restrict to messages with `timestampMilli` strictly greater than this value.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.SearchMessagesRequest(
            text=text,
            from_=utils.unmarshal(from_, Optional[List[str]]),
            in_=utils.unmarshal(in_, Optional[List[str]]),
            limit=limit,
            strategy=strategy,
            before_ts=before_ts,
            after_ts=after_ts,
        )

        req = self._build_request_async(
            method="POST",
            path="/searchMessages",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.SearchMessagesRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="searchMessages",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> (
            Awaitable[Optional[models.operations.SearchMessagesResponse]]
        ):
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            async def empty_result():
                return None

            next_cursor = JSONPath("$.content[-1].timestampMilli").parse(body)

            if len(next_cursor) == 0:
                return empty_result()

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return empty_result()
            results = JSONPath("$.content").parse(body)
            if len(results) == 0 or len(results[0]) == 0:
                return empty_result()

            return self.search_messages_async(
                text=text,
                from_=from_,
                in_=in_,
                limit=limit,
                strategy=strategy,
                before_ts=next_cursor,
                after_ts=after_ts,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.operations.SearchMessagesResponse(
                result=unmarshal_json_response(models.SearchMessagesResult, http_res),
                next=next_func,
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def delete_message(
        self,
        *,
        message_id: str,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Delete a message by ID

        :param message_id:
        :param channel_id:
        :param channel:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.DeleteMessageRequest(
            message_id=message_id,
            channel_id=channel_id,
            channel=channel,
        )

        req = self._build_request(
            method="DELETE",
            path="/deleteMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def delete_message_async(
        self,
        *,
        message_id: str,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Delete a message by ID

        :param message_id:
        :param channel_id:
        :param channel:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.DeleteMessageRequest(
            message_id=message_id,
            channel_id=channel_id,
            channel=channel,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/deleteMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def list_messages(
        self,
        *,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = 100,
        strategy: Optional[models.ListMessagesStrategy] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.operations.ListMessagesResponse]:
        r"""List messages in a channel

        Retrieves a paginated list of messages from a channel. Pagination is
        cursor-based: pass the `id` of the last returned message as the next
        `cursor`. `hasMoreBefore` / `hasMoreAfter` indicate which direction
        still has messages relative to the cursor.


        :param channel_id:
        :param channel:
        :param cursor: ID of the last message on the previous page.
        :param limit:
        :param strategy: Pagination direction relative to `cursor`. When `cursor` is omitted the
            server returns the most-recent page regardless of `strategy`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.ListMessagesRequest(
            channel_id=channel_id,
            channel=channel,
            cursor=cursor,
            limit=limit,
            strategy=strategy,
        )

        req = self._build_request(
            method="GET",
            path="/listMessages",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listMessages",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> Optional[models.operations.ListMessagesResponse]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            next_cursor = JSONPath("$.messages[-1].id").parse(body)

            if len(next_cursor) == 0:
                return None

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return None
            results = JSONPath("$.messages").parse(body)
            if len(results) == 0 or len(results[0]) == 0:
                return None

            return self.list_messages(
                channel_id=channel_id,
                channel=channel,
                cursor=next_cursor,
                limit=limit,
                strategy=strategy,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.operations.ListMessagesResponse(
                result=unmarshal_json_response(models.MessageList, http_res),
                next=next_func,
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def list_messages_async(
        self,
        *,
        channel_id: Optional[str] = None,
        channel: Optional[str] = None,
        cursor: Optional[str] = None,
        limit: Optional[int] = 100,
        strategy: Optional[models.ListMessagesStrategy] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.operations.ListMessagesResponse]:
        r"""List messages in a channel

        Retrieves a paginated list of messages from a channel. Pagination is
        cursor-based: pass the `id` of the last returned message as the next
        `cursor`. `hasMoreBefore` / `hasMoreAfter` indicate which direction
        still has messages relative to the cursor.


        :param channel_id:
        :param channel:
        :param cursor: ID of the last message on the previous page.
        :param limit:
        :param strategy: Pagination direction relative to `cursor`. When `cursor` is omitted the
            server returns the most-recent page regardless of `strategy`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.ListMessagesRequest(
            channel_id=channel_id,
            channel=channel,
            cursor=cursor,
            limit=limit,
            strategy=strategy,
        )

        req = self._build_request_async(
            method="GET",
            path="/listMessages",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 30000, 1.5, 60000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listMessages",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> Awaitable[Optional[models.operations.ListMessagesResponse]]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            async def empty_result():
                return None

            next_cursor = JSONPath("$.messages[-1].id").parse(body)

            if len(next_cursor) == 0:
                return empty_result()

            next_cursor = next_cursor[0]
            if next_cursor is None or str(next_cursor).strip() == "":
                return empty_result()
            results = JSONPath("$.messages").parse(body)
            if len(results) == 0 or len(results[0]) == 0:
                return empty_result()

            return self.list_messages_async(
                channel_id=channel_id,
                channel=channel,
                cursor=next_cursor,
                limit=limit,
                strategy=strategy,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return models.operations.ListMessagesResponse(
                result=unmarshal_json_response(models.MessageList, http_res),
                next=next_func,
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def add_reaction(
        self,
        *,
        message_id: str,
        reaction: str,
        channel_id: Optional[str] = None,
        skin_tone: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.operations.AddReactionResponse:
        r"""Add a reaction (emoji) to a message

        Adds a reaction code in `:emoji_name:` form. Pumble enforces the
        colon-wrapped form server-side; bare names (e.g. `+1`) return 403.


        :param message_id:
        :param reaction: Emoji code in `:emoji_name:` form (e.g. `:+1:`).
        :param channel_id:
        :param skin_tone:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.AddReactionRequest(
            message_id=message_id,
            channel_id=channel_id,
            reaction=reaction,
            skin_tone=skin_tone,
        )

        req = self._build_request(
            method="POST",
            path="/addReaction",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.AddReactionRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="addReaction",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.operations.AddReactionResponse, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def add_reaction_async(
        self,
        *,
        message_id: str,
        reaction: str,
        channel_id: Optional[str] = None,
        skin_tone: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.operations.AddReactionResponse:
        r"""Add a reaction (emoji) to a message

        Adds a reaction code in `:emoji_name:` form. Pumble enforces the
        colon-wrapped form server-side; bare names (e.g. `+1`) return 403.


        :param message_id:
        :param reaction: Emoji code in `:emoji_name:` form (e.g. `:+1:`).
        :param channel_id:
        :param skin_tone:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.AddReactionRequest(
            message_id=message_id,
            channel_id=channel_id,
            reaction=reaction,
            skin_tone=skin_tone,
        )

        req = self._build_request_async(
            method="POST",
            path="/addReaction",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.AddReactionRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="addReaction",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.operations.AddReactionResponse, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def remove_reaction(
        self,
        *,
        message_id: str,
        reaction: str,
        channel_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.operations.RemoveReactionResponse:
        r"""Remove a reaction from a message

        :param message_id:
        :param reaction: Emoji code in `:emoji_name:` form to remove.
        :param channel_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.RemoveReactionRequest(
            message_id=message_id,
            channel_id=channel_id,
            reaction=reaction,
        )

        req = self._build_request(
            method="DELETE",
            path="/removeReaction",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="removeReaction",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.operations.RemoveReactionResponse, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def remove_reaction_async(
        self,
        *,
        message_id: str,
        reaction: str,
        channel_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.operations.RemoveReactionResponse:
        r"""Remove a reaction from a message

        :param message_id:
        :param reaction: Emoji code in `:emoji_name:` form to remove.
        :param channel_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.RemoveReactionRequest(
            message_id=message_id,
            channel_id=channel_id,
            reaction=reaction,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/removeReaction",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="removeReaction",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.operations.RemoveReactionResponse, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    def edit_message(
        self,
        *,
        message_id: str,
        channel_id: str,
        text: str,
        blocks: Optional[
            Union[Iterable[models.MessageBlock], Iterable[models.MessageBlockTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Edit a message

        :param message_id:
        :param channel_id:
        :param text:
        :param blocks:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.EditMessageRequest(
            message_id=message_id,
            channel_id=channel_id,
            text=text,
            blocks=utils.get_pydantic_model(
                blocks, Optional[List[models.MessageBlock]]
            ),
        )

        req = self._build_request(
            method="POST",
            path="/editMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.EditMessageRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="editMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)

    async def edit_message_async(
        self,
        *,
        message_id: str,
        channel_id: str,
        text: str,
        blocks: Optional[
            Union[Iterable[models.MessageBlock], Iterable[models.MessageBlockTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Edit a message

        :param message_id:
        :param channel_id:
        :param text:
        :param blocks:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.operations.EditMessageRequest(
            message_id=message_id,
            channel_id=channel_id,
            text=text,
            blocks=utils.get_pydantic_model(
                blocks, Optional[List[models.MessageBlock]]
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/editMessage",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.operations.EditMessageRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="editMessage",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["Messages"],
                extensions={"x-sdk-no-write-retries": True},
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(models.errors.ErrorUnion, http_res)
            raise models.errors.Error(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.errors.PumbleSDKError(
                "API error occurred", http_res, http_res_text
            )

        raise models.errors.PumbleSDKError("Unexpected response received", http_res)
